#Suppose we want to do numbering for our month ( Jan, Feb, Marc,
#  ….June), so we declare the variable i that enumerate the
#  numbers while m will print the number of month in list.

#use a for loop over a collection
Months = ["Jan", "Feb", "Mar", "April", "May", "June"]
for i, m in enumerate(Months):
    print(i, m)

# use the break and continue statements

    #for x in range (10,20):
    #if (x == 15): break
    #if (x % 5 == 0) : continue
    #print x
